{
  "name": "vue-spa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "local": "vite --mode localstack",
    "build": "vite build --mode prod",
    "build:dev": "vite build --mode dev",
    "build:uat": "vite build --mode uat",
    "build:localstack": "vite build --mode localstack",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^6.0.0"
  }
}
